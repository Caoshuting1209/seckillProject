<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shuting.seckillproject.mapper.SeckillMapper">
    <select id="findUnstartSeckill" resultType="com.shuting.seckillproject.entity.Seckills">
        SELECT * FROM seckill WHERE now() BETWEEN start_time AND end_time and status = 0;
    </select>

    <update id="updateStatus" parameterType="com.shuting.seckillproject.entity.Seckills">
        UPDATE seckill SET good_id = #{goodId}, good_count = #{goodCount},
                           start_time = #{startTime}, end_time = #{endTime},
                           status = #{status}, current_price = #{currentPrice}
                       WHERE id = #{id};
    </update>
</mapper>